name: Node - Azure IAC CD workflow
on:
  workflow_call:
    inputs:
      allowed-dirs:
        description: 'Directories where Cloud functions are located. Split by comma.'
        required: true
        type: string
      acr-name:
        description: 'Azure Container Registry name.'
        required: true
        type: string
      shared-rg-name:
        description: 'Shared resource group name.'
        required: true
        type: string
      rg-name:
        description: 'Resource group name.'
        required: true
        type: string
      docker-image-tags:
        description: 'Docker image tags.'
        required: true
        type: string
      function-ids:
        description: 'Function ids.'
        required: true
        type: string
    secrets:
      azure-tenant-id:
        description: ''
        required: true
      azure-client-id:
        description: ''
        required: true
      azure-shared-subscription-id:
        description: ''
        required: true
      azure-subscription-id:
        description: ''
        required: true

jobs:
  changes:
    name: Look for changes in Cloud Functions
    runs-on: ubuntu-latest
    outputs:
      directories: ${{ steps.filter.outputs.directories }}
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Check changed files
        id: diff
        run: |
          if [ $GITHUB_BASE_REF ]; then
            # Pull Request
            git fetch origin $GITHUB_BASE_REF --depth=1
            export DIFF=$( git diff --name-only origin/$GITHUB_BASE_REF $GITHUB_SHA )
            echo "Diff between origin/$GITHUB_BASE_REF and $GITHUB_SHA"
          else
            # Push
            git fetch origin ${{ github.event.before }} --depth=1
            export DIFF=$( git diff --diff-filter=d --name-only ${{ github.event.before }} $GITHUB_SHA )
            echo "Diff between ${{ github.event.before }} and $GITHUB_SHA"
          fi
          echo "$DIFF"
          # Escape newlines (replace \n with %0A)
          echo "diff=$( echo "$DIFF" | sed ':a;N;$!ba;s/\n/%0A/g' )" >> $GITHUB_OUTPUT
      - name: Set matrix for build
        id: filter
        run: |
          DIFF="${{ steps.diff.outputs.diff }}"
          DIFF=$(printf '%b' "${DIFF//%0A/$'\n'}")

          if [ -z "$DIFF" ]; then
            echo "directories=[]" >> $GITHUB_OUTPUT
          else
            JSON="["
            # Loop by lines
            while read path; do

            # Set $directory to substring before third /
            directory="$( echo $path | cut -d'/' -f1-2 -s )"

            # only allow directories specified in allowed-dirs
            IFS=',' read -ra ALLOWED <<< "${{ inputs.allowed-dirs }}"
            for i in "${ALLOWED[@]}"; do
              if [[ "$directory" == "$i"* ]]; then
                # Add build to the matrix only if it is not already included
                JSONline="\"$directory\","

                if [[ "$JSON" != *"$JSONline"* ]]; then
                  JSON="$JSON$JSONline"
                fi
              fi
            done
            done <<< "$DIFF"

            # Remove last "," and add closing brackets
            if [[ $JSON == *, ]]; then
              JSON="${JSON%?}"
            fi
            JSON="$JSON]"
            echo $JSON

            # Set output
            echo "directories=$( echo "$JSON" )" >> $GITHUB_OUTPUT
          fi

  build-and-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.directories != '[]' && needs.changes.outputs.directories != '' }}
    strategy:
      matrix:
        directory: ${{ fromJSON(needs.changes.outputs.directories) }}
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          tenant-id: ${{ secrets.azure-tenant-id }}
          client-id: ${{ secrets.azure-client-id }}
          subscription-id: ${{ secrets.azure-shared-subscription-id }}

      - name: Resolve a Docker image
        run: |
          DOCKER_IMAGE_TAG=$(echo '${{ inputs.docker-image-tags }}' | jq -r ".[\"${{ matrix.directory }}\"]")
          echo "DOCKER_IMAGE_TAG=$DOCKER_IMAGE_TAG" >> $GITHUB_ENV

      - name: Resolve a Function id
        run: |
          FUNCTION_ID=$(echo '${{ inputs.function-ids }}' | jq -r ".[\"${{ matrix.directory }}\"]")
          echo "FUNCTION_ID=$FUNCTION_ID" >> $GITHUB_ENV

      - name: Build Docker image
        run: az acr build -r ${{ inputs.acr-name }} -g ${{ inputs.shared-rg-name }} -f Dockerfile -t ${{ env.DOCKER_IMAGE_TAG }} . --platform linux/amd64
        working-directory: ${{ matrix.directory }}

      - name: Change subscription
        run: az account set -s ${{ secrets.azure-subscription-id }}

      - name: Update the FunctionApp
        run: az functionapp config container set -n ${{ env.FUNCTION_ID }} -g ${{ inputs.rg-name }} --image ${{ env.DOCKER_IMAGE_TAG }}
